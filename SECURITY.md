# Security

Do not commit credentials, private source archives, restricted datasets, or personally sensitive research material.

Ingestion implementations must treat external artifacts as untrusted input and should verify paths, sizes, content type, checksums, and archive extraction boundaries before parsing.
