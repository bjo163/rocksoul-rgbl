# Corpus Studio

`apps/studio` is a Git-first curation interface. It generates reviewable v0.1 curation overlays in the browser; it has no database, persistence layer, or hidden canonical state. The generated overlay must be committed to a dataset/recipe and reviewed through the normal repository validation and pull-request process.
