# P12-007–009 — OSHB/WLC

The repository pins `openscriptures/morphhb` `master` at commit `3d15126fb1ef74867fc1434be1942e837932691f`. The downloaded archive is SHA-256 `f979b5357fb18391928cd42ee1b95594db6e4f71d8da4e893c6c18f2688093a6` and 20,824,328 bytes. Each bundled XML file has an additional SHA-256 entry in the source manifest.

The WLC Hebrew text is stored source-preserving at verse level in `textual.content` records. OSHB `w` token IDs, lemma, morphology, and optional `n` values are stored in separate morphology evidence records, linked to the corresponding source content. No NFC normalization is applied.

Rights are mixed: the WLC text is marked public domain by OSHB, while OSHB lemma/morphology annotations are attributed under CC BY 4.0. The bundled artifact therefore retains attribution and uses `CC-BY-4.0` as its redistribution license expression.
