# Example lines ingestion recipe

Rights-safe synthetic fixture for the P4 ingestion framework. It uses a local pinned file, deterministic parser/normalizer/mapper hooks, an explicit curation overlay, and no network access.
